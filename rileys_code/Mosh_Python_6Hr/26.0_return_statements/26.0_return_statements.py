# By default, functions return "None"
# Using return we can make a function return anything
def square(number):
    return number * number


# Result will have the value that the function returns
result = square(3)
print(result)

# This will aslo print what the function returns
print(square(3))